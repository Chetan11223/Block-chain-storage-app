/** Stub for MetaMask SDK optional RN dependency (not used in web builds). */
module.exports = {
  default: {
    getItem: async () => null,
    setItem: async () => {},
    removeItem: async () => {},
  },
};
